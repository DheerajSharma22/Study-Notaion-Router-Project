import React from "react";

const Design = (props) => {
  return (
    <div className="flex items-center justify-center text-richblack-100 text-3xl h-full min-h-screen">
      {props.title}
    </div>
  );
};

export default Design;
